/**
 * Integrations layout - minimal wrapper for integration pages (error page).
 * These pages have self-contained styling.
 */
export default function IntegrationsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
