/**
 * Re-export barrel — the canonical storage adapter now lives at
 * `lib/adapters/storage/`. This file exists only for backwards compatibility.
 */
export { getStorage, mimeToExt } from "@/lib/adapters/storage";
