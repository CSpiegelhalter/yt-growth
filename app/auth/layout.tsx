/**
 * Auth layout - minimal wrapper for auth pages.
 * Auth pages have their own full-page styling with centered cards.
 * No header or footer needed.
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
